{
  "name": "easy-break-energy-switcher",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Test suite (TDD scaffold) for the Easy Break energy contract switcher, per ARCH_SPEC.md",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
