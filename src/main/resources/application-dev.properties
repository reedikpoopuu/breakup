# Local development only - never merge these into application.properties directly.
# Activated via --spring.profiles.active=dev, same as the rest of this session's dev runs.
spring.h2.console.enabled=true
