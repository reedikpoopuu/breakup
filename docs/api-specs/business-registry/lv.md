# Latvia — business registry — awaiting spec from PM

Status: **awaiting spec from PM** (`PM_ANSWERS.txt` #4 — local business registry has an API;
mock until credentials/spec are supplied). Provider not yet named — see `PM_QUESTIONS.txt`.

Referenced from `ARCH_SPEC.md` §5, consumed by the representative-rights verification service in
`ARCHITECTURE.md` §3.2. Needs: provider name, base URL, auth, company-lookup call, and
representative-rights response shape.
